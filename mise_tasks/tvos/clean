#!/usr/bin/env zsh
#MISE description="Clean the tvOS build directory"
set -euo pipefail

if [ -d "$TVOS_BUILD_DIR" ]; then
  xcodebuild -project "$TVOS_BUILD_DIR/StepMania.xcodeproj" \
    -scheme ITGmania \
    clean 2>/dev/null || true
fi
rm -rf "$TVOS_BUILD_DIR"
echo "Cleaned $TVOS_BUILD_DIR"
